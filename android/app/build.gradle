plugins {
    id "com.android.application"
    // START: FlutterFire Configuration
    id 'com.google.gms.google-services'
    // END: FlutterFire Configuration
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.app_ascs"
    compileSdk = 36
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"  
    }

    defaultConfig {

        // en google-services.json
        applicationId = "com.example.app_ascs"
        minSdk = 24 
        targetSdk = 34
        versionCode = flutter.versionCode
        versionName = flutter.versionName

        // Usa el Web Client ID del google-services.json
        // Formato: com.googleusercontent.apps.[WEB_CLIENT_ID_PREFIX]
        manifestPlaceholders += [
            'appAuthRedirectScheme': 'com.googleusercontent.apps.764495160503-c08scuv6gkqdb6h1h76hvo44l2jr3ktp'
        ]
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
            // Optimizaciones para producción 
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    // Configuración específica para Google Drive
    packagingOptions {
        resources.excludes += "META-INF/*"
    }
}

flutter {
    source = "../.."
}

dependencies {
    implementation platform('com.google.firebase:firebase-bom:32.8.0')
    implementation 'com.google.firebase:firebase-analytics'
    implementation 'com.google.android.gms:play-services-auth:21.1.0'
}